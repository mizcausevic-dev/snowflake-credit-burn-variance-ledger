# Security

Do not commit Snowflake account identifiers, warehouse URLs, access tokens, private keys, query text from sensitive workloads, customer identifiers, or raw ACCOUNT_USAGE exports.

This repository uses synthetic fixtures only. If adapting it to a real estate, aggregate query telemetry before export and avoid publishing raw query text.
